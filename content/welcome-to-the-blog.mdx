---
title: "Welcome to the blog"
date: "2026-04-30"
description: "A short introduction to the new MDX-based blog system in this playground project."
---

This blog is powered by MDX, so posts can mix markdown with React-friendly formatting and still stay fully server-rendered.

## Why this setup

- Posts live in the `content` directory.
- Frontmatter is parsed at build time.
- Pages are statically generated for fast delivery.

```ts
export const example = "MDX works well for technical writing";
```

You can add more posts by dropping additional `.mdx` files into the same folder.
