generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model VisitorCounter {
  id        Int      @id
  count     Int      @default(0)
  updatedAt DateTime @updatedAt
}